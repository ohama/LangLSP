// =============================================
// Diagnostics (Error Detection) Test
// =============================================
// 실시간 오류 표시를 테스트하세요.
//
// 확인 항목:
//   1. 문법 오류: 빨간 밑줄 + 오류 메시지
//   2. 타입 오류: 빨간 밑줄 + 타입 불일치 메시지
//   3. 저장 없이 타이핑 중 업데이트

// --- 정상 코드 (오류 없음) ---
let x = 42 in
let y = x + 1 in
let z = if y > 0 then "positive" else "negative" in

// --- 아래 주석을 한 번에 하나씩 해제하면 오류가 표시됩니다 ---
// (주의: 반드시 하나만 해제하세요. 여러 개 해제하면 첫 오류만 표시됩니다)

// 문법 오류 (주석 해제하여 테스트):
// let a = in

// 타입 오류 (주석 해제하여 테스트):
// let b = 1 + true in
// let c = if 42 then 1 else 2 in
// let d = 1 + "hello" in

z
